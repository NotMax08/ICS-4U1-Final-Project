import greenfoot.*;

public class Message extends Actor {
    public Message(String imageName) {
        setImage(imageName);
    }
    
    public Message(GreenfootImage image) {
        setImage(image);
    }
}